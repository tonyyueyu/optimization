FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Copy the rest of the code
COPY . .

# Expose Vite's default port
EXPOSE 5173

# Start the app with --host so it is accessible outside the Docker container
CMD ["npm", "run", "dev", "--", "--host"]